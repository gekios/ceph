tasks:
        - exec:
                client.salt_master:
                        - 'ceph --version || true'
                        - 'ceph -s || true'
        - deepsea.repository:
                client.salt_master:
        - deepsea.reboot:
                client.salt_master:
        - deepsea:
                upgrade_install: package
        - exec:
                client.salt_master:
                        - 'deepsea --version || true'
                        - 'salt-run deepsea.version || true'
                        - 'ceph --version || true'
                        - 'ceph -s || true'
        - deepsea.repository:
                mon.a:
        - deepsea.reboot:
                mon.a:
        - exec:
                client.salt_master:
                        - 'ceph --version || true'
                        - 'ceph -s || true'
